{
  "name": "termchat-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "termchat — the terminal client (TUI, presence daemon, agent hooks). Talks to the termchat service; your prompts never leave your machine.",
  "homepage": "https://termchat.sh",
  "repository": {
    "type": "git",
    "url": "https://github.com/OviatoHQ/termchat-client.git"
  },
  "license": "Apache-2.0",
  "workspaces": ["apps/cli", "packages/protocol"],
  "scripts": {
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "bun test packages/protocol apps/cli"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.6.3"
  }
}
